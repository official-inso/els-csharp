# Static facade (EN)

Demonstrates the ambient `Sdk.Init` / `Sdk.CaptureError` pattern — useful when
dependency injection is overkill (one-off scripts, glue code, console tools).

```bash
export ELS_API_KEY=your-api-key
dotnet run --project examples/en/StaticFacade
```
