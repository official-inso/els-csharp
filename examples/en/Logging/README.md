# Logging example (EN)

Generic Host app that demonstrates routing `Microsoft.Extensions.Logging` to ELS.

```bash
export ELS__Endpoint=https://api.insoweb.ru/els
export ELS__ApiKey=your-api-key
dotnet run --project examples/en/Logging
```
