# Custom HttpClient (EN)

Routes ELS traffic through a custom `HttpClient` with a logging
`DelegatingHandler`. Replace with proxy / SOCKS / mTLS handlers as needed.

```bash
dotnet run --project examples/en/CustomHttpClient
```
