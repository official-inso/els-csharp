# Basic example (EN)

Console application that exercises the most common SDK surface: async capture,
synchronous send for critical errors, a health check, and graceful shutdown.

```bash
export ELS_ENDPOINT=https://api.example.com/els
export ELS_API_KEY=your-api-key
dotnet run --project examples/en/Basic
```
