# Filtering (EN)

Combines `MinLevel`, `SampleRate`, sync `BeforeSend` (mask emails) and async
`BeforeSendAsync` (block tenants). Demonstrates that critical-level entries
always pass.

```bash
dotnet run --project examples/en/Filtering
```
