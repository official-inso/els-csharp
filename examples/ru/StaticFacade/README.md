# Статический фасад (RU)

Демонстрирует ambient-паттерн `Sdk.Init` / `Sdk.CaptureError` — полезно когда
DI избыточен (разовые скрипты, glue-код, консольные утилиты).

```bash
export ELS_API_KEY=ваш-api-ключ
dotnet run --project examples/ru/StaticFacade
```
