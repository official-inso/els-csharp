# Custom HttpClient (RU)

Пускает трафик ELS через кастомный `HttpClient` с логирующим
`DelegatingHandler`. Замените на proxy / SOCKS / mTLS по необходимости.

```bash
dotnet run --project examples/ru/CustomHttpClient
```
