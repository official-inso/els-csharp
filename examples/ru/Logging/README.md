# Пример с ILogger (RU)

Generic Host приложение, маршрутизирующее `Microsoft.Extensions.Logging` в ELS.

```bash
export ELS__Endpoint=https://api.example.com/els
export ELS__ApiKey=ваш-api-ключ
dotnet run --project examples/ru/Logging
```
